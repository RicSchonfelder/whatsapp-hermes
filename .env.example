# Access control: comma-separated phone numbers (digits only, country code, no +).
# Only messages from these numbers enter the message buffer.
# Use "*" to allow all senders (dev only — logs a warning).
WHATSAPP_ALLOWED_NUMBERS=

# Optional: max messages kept in the in-memory ring buffer (default 200)
WHATSAPP_BUFFER_SIZE=200
