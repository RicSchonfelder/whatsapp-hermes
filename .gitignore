# Dependencies
node_modules/

# WhatsApp auth/session — NEVER commit (grants full account access)
wa_auth/
*.session

# Logs
*.log
logs/

# Env
.env

# Manual pairing helper artifacts (generated, not part of the project)
qr_pair.png
qr_capture.mjs
qr_capture.log

# Runtime message cache (voice notes + transcripts) — local only, never commit
wa_cache/

# OS
.DS_Store
Thumbs.db
desktop.ini
